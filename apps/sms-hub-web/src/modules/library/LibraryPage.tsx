import PlaceholderPage from '@/components/PlaceholderPage';

export default function LibraryPage() {
  return <PlaceholderPage title="Biblioteca" description="Fabricantes, modelos e comandos — próxima entrega." />;
}
